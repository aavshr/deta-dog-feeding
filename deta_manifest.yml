v: 0
micros:
  - name: mermaid
    src: mermaid
    engine: svelte-kit
    primary: true
    commands:
      - npm i esbuild-linux-64
    presets:
      env:
        - name: PORT
          description: port for node adapter
          default: 8080