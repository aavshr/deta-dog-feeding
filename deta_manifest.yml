v: 0
micros:
  - name: mermaid
    src: ./mermaid
    engine: svelte-kit
    primary: true
    presets:
      env:
        - name: PORT
          description: port for node adapter
          default: 8080